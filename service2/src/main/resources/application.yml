server:
    port: 8093

spring:
    application:
        name: service2
eureka:
    client:  
        service-url:
            default-zone: http://localhost:8761/eureka/
instance:
    hostname: localhost